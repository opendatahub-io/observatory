# M1: Bootstrap

**Phase:** 1 — Core API + Static Inventory
**Status:** Pending

## Definition

The application exists as a running container with a seeded database, pipeline CRUD API, and a React shell that renders a status board with grey cards. No live data yet.

## Key Results

- [ ] FastAPI app starts and serves API + React static files
- [ ] SQLite schema covers all tables from the data model
- [ ] Pipeline CRUD works end-to-end (API → DB → UI)
- [ ] Status board renders pipeline cards from seed data
- [ ] Container builds and runs locally
- [ ] `/metrics` returns Prometheus metrics
