apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - route.yaml
  - pvc.yaml
  - configmap.yaml
  - secret.yaml

commonLabels:
  app.kubernetes.io/part-of: agentic-ci-observatory
