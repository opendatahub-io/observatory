apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: observatory-data
  labels:
    app.kubernetes.io/name: observatory
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
